<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

    </data>

    <RelativeLayout
        android:id="@+id/activity_user_followers"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context="com.leo.genesistask.view.activity.UserFollowersActivity">

        <android.support.v7.widget.RecyclerView
            android:id="@+id/rv_followers"
            android:layout_width="match_parent"
            android:layout_height="match_parent">
        </android.support.v7.widget.RecyclerView>

        <TextView
            android:id="@+id/tv_empty"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:gravity="center"
            android:text="@string/empty_view_followers"
            android:textSize="@dimen/text_regular_size"
            android:visibility="gone"
            app:font="@{`Roboto-Regular.ttf`}"
            />

        <Button
            android:id="@+id/btn_retry"
            style="@style/ButtonStyle.Green"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/tv_empty"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="15dp"
            android:text="@string/action_retry"
            android:visibility="gone"
            app:font="@{`Roboto-Regular.ttf`}"
            />

    </RelativeLayout>
</layout>

